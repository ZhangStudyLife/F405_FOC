# FOC 通用化与代码清理

## 目标

在本仓库内清理已证明无用的代码，并以最少代码理清 FOC 模块边界。保持 20 kHz 有感电流环、1 kHz 速度/位置控制、2 kHz USB 日志、20 Hz UART 安全监测和可选 FOC_CAPTURE 的现有行为。当前 MT6835 与 5010 参数作为默认实现；为后续替换留静态接口，不做运行时识别或配置。

## 工作要求

- 审查活动固件、测试、bench 工具、旧代码和生成代码的引用、构建及重新生成关系；删除确认无调用、无读取、无构建/测试/生成依赖的变量、函数、语句及历史实现。每项删除须有引用依据。
- 将编码器驱动、FOC 电流环、速度控制、位置控制、命令解析、遥测组帧/调度、USB/UART 收发和电机/编码器参数保持为清晰职责。`app` 只负责编排；算法不依赖 USB，底层驱动不依赖应用。
- 5010 的电机限制与环路参数、MT6835 的方向和安装补偿集中管理；当前算法读取静态配置，不增加多余配置层或通用框架。
- 维持 USB 15-float/2 kHz、UART 20 Hz 安全帧和命令行为兼容；不得在 20 kHz 实时路径增加阻塞传输、格式化输出或动态分配。
- 不实现无感估算；保留 FOC_CAPTURE 的 20 kHz 离线输入和编码器真值，用于以后单独增加估算模块。
- 对 CubeMX/HAL 生成物、`tests/legacy` 和旧协议逐项确认依赖后再删除；不因“看起来旧”而删除仍被生成、构建或验证使用的内容。
- 遵守仓库 `AGENTS.md`：优先少代码、最小改动，不做推测性抽象，不增加未要求的功能。

## 验收

- 当前主机 C 测试、`python tools/bench/selftest.py`、`python tools/bench/test_recovery.py` 和 `git diff --check` 全部通过。
- Debug、Release、FOC_CAPTURE 构建成功，无编译/链接错误或新增警告。
- ST-Link 烧录 Release 成功；台架验证命令、编码器、低速/高速、正反转、速度/位置模式、USB 连续性、UART 安全状态，以及 FOC_CAPTURE 采满、停机和导出解析。
- 测试期间 ADC、编码器、UART、USB 和 FOC 时序无错误；测试报告和原始数据写入 `build/bench_debug/<时间戳>/`。
- 收尾时电机停止、电源输出关闭、板卡无故障；记录最终固件、测试结果、未覆盖项和工作区状态。

## 继续与停止

仅在存在可复现测试/构建失败、台架丢帧或错误、实时性异常、离线采集不完整，或可证明的模块反向依赖/重复职责时继续迭代。每轮先保留现场与证据，只修改能解决该问题的最小代码，再重复相关回归。

所有验收项完成、台架安全收尾，且没有可复现问题或有证据的结构性耦合时停止。没有新证据就不继续“为了更通用”或单纯追求更少行数而改代码。若受硬件或工具限制无法完成某项，记录证据和剩余风险，不把未验证项写成通过。

## 交付

提交目标文档、必要的固件/工具/测试/文档修改，以及包含实际覆盖工况、结果、限制和数据路径的台架报告；使用一个概括最终改动和验证范围的 commit。
